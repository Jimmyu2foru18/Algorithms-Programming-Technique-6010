## Part 4 Final Exercise Question
You can get user input in python in the following way

username = input("Enter username:")
print("Username is: " + username)

try it in the jupyter notebook! 

Now can you write a program in the jupyter notebook that asks the user how many people are in a class, and once the user enters that number asks the user to enter all the email addresses of the people in class and saves them in a list. The following is an example

>>Enter the number of people in the class

>>2

>>Enter a student email address

jr@gmails.com

>>Enter a student email address

kb@yahoos.com

[jr@gmails.com, kb@yahoos.com]

Now redo the previous question but ask the user to enter the students names and email addresses and then save them in a dictionary with the users name being the key and the email addresses being the value.

--- 

### Next 

Try to go through the following webpage

https://realpython.com/python-mutable-vs-immutable-types/

now write some code that takes an integer and updates its value to its current value plus 3. Run this code in python tutor-- what happens-- does it create a new integer or is the same integer (with the same ID) in memory. You can also try this in jupyter notebook with the id method we learned in class. Is an integer immuteable or muteable? Attach a picture of the python tutor exercise

Use python tutor for memory visualization but Now with a List-- create a list and append a value to it, extend it by a given number of values. Did the ID change here? Does that mean lists are muteable or immuteable?

attach a picture of this from python tutor.